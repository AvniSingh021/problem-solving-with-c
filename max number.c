#include<stdio.h>
int max();
  int main(){
	int a,b;
	printf("enter the mix no:");
	scanf("%d%d",&a,&b);
	if(a>b){
		printf("a is max number");
	}
	else
	if("b is max number");
	void main(){
		max();
	}
}
